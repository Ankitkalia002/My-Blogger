<%= form_for(@customer_card) do |f| %>
  <% if @customer_card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@customer_card.errors.count, "error") %> prohibited this customer_card from being saved:</h2>

      <ul>
      <% @customer_card.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
